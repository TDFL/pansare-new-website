version: 0.2

phases:
  install:
    commands:
      - echo "Installing validator."
      - npm install -g htmlhint

  build:
    commands:
      - echo "Validating HTML files."
      - htmlhint "**/*.html"

artifacts:
  files:
    - '**/*'
